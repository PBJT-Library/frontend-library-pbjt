name: Build Desktop App

on:
  push:
    tags:
      - 'v*' # Trigger on version tags like v1.0.0
  workflow_dispatch: # Allow manual trigger

jobs:
  build-windows:
    runs-on: windows-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable

      - name: Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: './frontend-library/src-tauri -> target'

      - name: Install dependencies
        working-directory: ./frontend-library
        run: bun install

      - name: Build Tauri app
        working-directory: ./frontend-library
        run: bun run tauri build

      - name: Upload Windows Installer (.exe)
        uses: actions/upload-artifact@v4
        with:
          name: windows-installer-exe
          path: frontend-library/src-tauri/target/release/bundle/nsis/*.exe

      - name: Upload Windows Installer (.msi)
        uses: actions/upload-artifact@v4
        with:
          name: windows-installer-msi
          path: frontend-library/src-tauri/target/release/bundle/msi/*.msi

      - name: Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v1
        with:
          files: |
            frontend-library/src-tauri/target/release/bundle/nsis/*.exe
            frontend-library/src-tauri/target/release/bundle/msi/*.msi
          draft: false
          prerelease: false
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
