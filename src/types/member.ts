// Member entity type definition
export interface Member {
    id: string;             // This IS the NIM (e.g., "23190001")
    name: string;
    study_program: string;  // Program studi
    semester: number;       // 1-14
}
